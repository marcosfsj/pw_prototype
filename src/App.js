import React, { Component } from 'react';
import Prototype from '../src/features/Prototype';

class App extends Component {
  render() {
    return (
      <div>
        <Prototype />
      </div>
    );
  }
}

export default App;
